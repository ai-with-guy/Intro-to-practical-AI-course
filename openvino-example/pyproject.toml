[project]
name = "openvino-workflow"
version = "0.1.0"
description = "Multi-model OpenVINO workflow for the Intro to Practical AI course"
requires-python = ">=3.11,<3.14"
dependencies = [
    "jupyter>=1.1.1",
    "matplotlib>=3.9",
    "numpy>=2.0",
    "opencv-python>=4.10",
    "openvino>=2025.2",
    "pillow>=11.0",
]

[tool.uv]
package = false
